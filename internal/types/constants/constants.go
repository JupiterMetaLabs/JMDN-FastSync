package constants

const(
	FAILURE = "failure"
)

const (
	SYNC_REQUEST = "state:priorsync:syncrequest"
	SYNC_REQUEST_RESPONSE = "state:priorsync:syncrequestresponse"
	
	SYNC_DATA = "state:sync:syncdata"
)

const(
	PriorSyncVersion = 1
)